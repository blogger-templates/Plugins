// license.min.js
(function(){
    if (document.readyState === "complete" || document.readyState === "interactive") {
        alert("Loaded after DOM ready");
    } else {
        document.addEventListener("DOMContentLoaded", function () {
            alert("Loaded before DOM ready");
        });
    }
})();
